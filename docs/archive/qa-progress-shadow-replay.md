# QA Session: shadow-mode replay (§11.4)

**Started**: 2026-05-21
**Mode**: Team (parallel reviewers via Agent tool)
**Scope**: 26 shadow-replay files (13 new api + 4 new dashboard + 9 modified shared), ~1135 new LOC
**Review Waves**: 2

> SCOPE FENCE: a concurrent session is editing billing/subscription in this checkout.
> Reviewers/fixers touch ONLY shadow-replay paths. Excluded: organizations_controller,
> admin_billing_service, subscription_provisioning_service (+ tests), api/.claude/.

**Status**: completed — all 13 findings fixed, independently re-verified by lead.

## DOD Checklist
- [x] All review waves completed
- [x] All findings documented
- [x] All findings fixed (zero deferred)
- [x] Scoped tests pass (Rails shadow tests; dashboard #7/#13 fixed by frontend-expert)
- [x] Build clean (rubocop, openapi:validate)
- [x] Progress file updated with final status

## Findings Summary
| Severity | Found | Fixed |
|----------|-------|-------|
| Critical | 0     | 0     |
| High     | 3     | 3     |
| Medium   | 4     | 4     |
| Low      | 6     | 6     |

## Findings Detail
| # | Severity | Description | File | Reviewer | Fix Status |
|---|----------|-------------|------|----------|------------|
| 1 | HIGH | Session sampling not org-scoped → latent cross-org prompt/summary leak | shadow_replay_service.rb / run_shadow_replay_job.rb | security | fixed |
| 2 | HIGH | Raw prompts/outputs exposed to any org viewer; bypasses `viewable_with_sensitive_context?` gate (same data trigger-classifiers gates) | shadow_replay_item_serializer.rb + controller | security | fixed |
| 3 | HIGH | No rate limit on 25×-Anthropic-call POST + no duplicate-run guard | shadow_replays_controller.rb + rack_attack.rb | security+contract | fixed |
| 4 | MED | `experiment?` create policy + project-resource-only limitation undocumented | shadow_replays_controller.rb / openapi | security | fixed |
| 5 | MED | FK `shadow_replay_items→llm_sessions` missing `on_delete: :nullify` | migration 20260521120000 | security | fixed |
| 6 | MED | OpenAPI: 404 undocumented on index/create | openapi.yaml | contract | fixed |
| 7 | MED | Dashboard `DivergenceHeadline` must guard null `divergence_rate` on completed/zero-session replay | shadow-replay-card.tsx | contract | fixed |
| 8 | LOW | Silent Anthropic failure (nil→"") recorded as completed+100%-diverged instead of failed item | run_shadow_replay_job.rb | security | fixed |
| 9 | LOW | Unbounded `initial_prompt` sent to Anthropic; truncate before send | run_shadow_replay_job.rb | security | fixed |
| 10 | LOW | `start!`/`complete!`/`fail!` use non-bang `update` (silent transition failure) | shadow_replay.rb | security | fixed |
| 11 | LOW | OpenAPI index advertises `_links` never emitted by serializer | openapi.yaml | contract | fixed |
| 12 | LOW | `sample_size` silent clamp of 0/invalid undocumented; verify max tracks `MAX_SAMPLE_SIZE` | openapi.yaml / service | contract | fixed |
| 13 | LOW(derived) | TS types + card must tolerate gated/absent raw item fields (from #2) | types/api.ts + shadow-replay-card.tsx | lead | fixed |

## Progress Log
- 2026-05-21 QA session started. Scope: 26 files, api + dashboard. Wave 1 = security/authz (code-reviewer) + API contract (api-designer).
- 2026-05-21 Wave 1 complete: 3 high, 4 medium, 6 low (deduped). Dispatching fixers: rails-expert (API: #1-6,8-12) + frontend-expert (dashboard: #7,13). Fixers also do domain code-quality review while fixing (merged Wave 2).
- 2026-05-21 API fixes (#1-6,8-12) complete. All resolved:
  - #1 `select_session_inputs` now requires `organization_id:` and filters on it; job passes `replay.organization_id`. New service test proves a same-resource/same-version session in a DIFFERENT org is excluded.
  - #2 Controller computes `include_sensitive` (nil user / platform_admin / owner|admin|manager role) and passes it through `ShadowReplaySerializer` → `ShadowReplayItemSerializer`; raw input_prompt/baseline_output/candidate_output emitted as null (keys retained) for non-privileged viewers. Controller tests: developer → null, owner/admin/platform-owner → populated.
  - #3 (a) Rack::Attack `shadow-replay/user` + `/ip` throttles reuse LLM_EXTRACTION_LIMIT; request-spec asserts 429. (b) `create` returns 409 when a pending/running replay exists for the same (resource, candidate); tests cover 409 + allow-when-completed/different-candidate.
  - #4 Controller comment + OpenAPI note: create uses `:experiment?` (privileged); only project-tier resources eligible (platform/org → 403).
  - #5 Migration FK `shadow_replay_items.llm_session_id` now `on_delete: :nullify`; redone via `db:migrate:redo:primary VERSION=20260521120000`. schema.rb regenerated by hand-merge to keep diff to shadow tables + version bump only (avoided unrelated pg-dump cosmetic drift).
  - #6 Added 404 (NotFound) to index + create; create also documents 409.
  - #8 Empty/blank candidate output (no raise) recorded as a `failed` item ("empty candidate output"), not completed+diverged. Job tests for empty-content + nil response.
  - #9 Prompt truncated to MAX_PROMPT_CHARS (8_000) before send (stored input_prompt keeps full original). Job test asserts wire cap.
  - #10 `start!`/`complete!`/`fail!` now `update!`; folded `model:` into `start!` to close the same silent-write gap.
  - #11 Removed `_links` from index/show shadow-replay OpenAPI response schemas (serializer emits none).
  - #12 Confirmed MAX_SAMPLE_SIZE == 25 (== OpenAPI maximum); dropped `minimum:1` and documented that out-of-range/invalid sample_size is clamped to 1..MAX.
  - Verify: 71 runs / 216 assertions, 0 failures (scoped suite incl. rate-limit sentinel + rack_attack integration green); rubocop 8 files 0 offenses; openapi:validate (and STRICT=1) pass; schema.rb diff = shadow tables + FKs (nullify) + version bump only.
